/**
 * Agent Scheduler Configuration (Deprecated)
 *
 * DEPRECATED: This file is no longer used. All agent scheduling is now handled
 * by Vercel Cron jobs (see src/app/api/cron/ and vercel.json).
 *
 * Historical note: This file previously used node-cron for local development
 * and self-hosted deployments. For current deployment options, see:
 * - Vercel Cron (production, via api/cron routes)
 * - AWS Lambda + CloudWatch Events
 * - GitHub Actions Workflows
 *
 * For more details, see SHIP_FASTER_INTEGRATION.md and the vercel.json configuration.
 */
